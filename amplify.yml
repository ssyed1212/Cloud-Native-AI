# Amplify build for Car Cloud Next.js app in this monorepo.
# Commands below handle both execution contexts:
# - repo root (Cloud-Native-AI)
# - app root already set to car-cloud/frontend
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20 || nvm install 20
        - '[ -d car-cloud/frontend ] && cd car-cloud/frontend || true'
        - npm ci
    build:
      commands:
        - '[ -d car-cloud/frontend ] && cd car-cloud/frontend || true'
        - npm run build
  artifacts:
    baseDirectory: car-cloud/frontend/.next
    files:
      - "**/*"
  cache:
    paths:
      - car-cloud/frontend/node_modules/**/*
